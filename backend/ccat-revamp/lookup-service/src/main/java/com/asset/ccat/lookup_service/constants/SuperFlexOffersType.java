package com.asset.ccat.lookup_service.constants;

public enum SuperFlexOffersType {

    ADDON(1),
    THRESHOLD(2);

    public int id;

    SuperFlexOffersType(int id) {
        this.id = id;
    }
}
